
This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

A static HTML/CSS habit tracker app that stores data in localStorage 
## Running the Site

Open `index.html` in a browser. No server, build step, or install required.

## Architecture
complex and complete app that is acceptable to all races genders and cultures no repetitiveness on style and ideas everything is unique and pleasng




## Styling Conventions

All colors reference CSS variables (`--red`, `--black`, `--grey`, etc.) defined in `:root` — don't hardcode hex values. Font stacks: `var(--font-jp)` for Japanese/display text, `var(--font-en)` for body copy.
